- Описать HTTP хендлер, который из входящего запроса получает
все хеддеры и выводит их на экран
- В HTTP запросе указать свой кастомный хеддер, в котором
передаётся ваше имя
- Изнутри HTTP хендлера выводить приветствие этому имени
